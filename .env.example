# Copy to `.env.local` and adjust for local development.

# Nametag of the chess bot service that handles all bot opponents.
# Defaults to `chess-bot` (production) when unset.
VITE_CHESS_BOT_NAMETAG=chess-bot
